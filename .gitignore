node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local SQLite (Phase 13) — production lives in the Docker volume mounted
# at /data; dev falls back to a repo-relative path that should never be
# committed.
local-data/
